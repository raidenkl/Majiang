{
  "name": "majiang-sea",
  "version": "2.5.3",
  "description": "電脳麻将 - Node SEA 单文件打包（内嵌完整网络对战服务器，双击即局域网主机）",
  "private": true,
  "scripts": {
    "dev": "node server.js ../dist",
    "build:zip": "node build.js",
    "build:blob": "node --experimental-sea-config sea-config.json",
    "build:bin": "node bin.js",
    "build": "npm run build:server && npm run build:zip && npm run build:blob && npm run build:bin",
    "build:server": "node ../node_modules/webpack/bin/webpack.js --config ../webpack.server.js"
  },
  "devDependencies": {
    "postject": "1.0.0-alpha.6"
  }
}
